# 삼삼엠투 크롤러 사용 가이드

## 프로그램 실행 방법

### 방법 1: EXE 파일 실행 (일반 사용자)
1. `33m2크롤러.exe` 파일을 더블클릭합니다.
2. Windows 보안 경고가 나타나면 "자세한 정보" → "실행"을 클릭합니다.
3. 프로그램 창이 나타나면 사용 준비 완료입니다.

### 방법 2: Python으로 실행 (개발자)
```bash
# 가상환경 활성화
venv\Scripts\activate

# 프로그램 실행
python main.py
```

## 각 버튼 사용법

### [홈페이지 접속] 버튼
- **기능**: Chrome 브라우저를 열고 삼삼엠투 검색 페이지로 이동합니다.
- **사용법**: 프로그램 시작 후 가장 먼저 클릭하세요.
- **상태 표시**:
  - "브라우저 시작 중..." → Chrome 드라이버 로딩
  - "페이지 접속 중..." → 웹사이트 연결
  - "접속 완료" → 크롤링 준비 완료

### [크롤링 시작] 버튼
- **기능**: 설정된 개수만큼 숙소 정보를 수집합니다.
- **사용법**: "접속 완료" 상태에서만 활성화됩니다.
- **진행 중**:
  - 진행률 바가 실시간으로 업데이트됩니다.
  - 로그 창에서 수집 현황을 확인할 수 있습니다.
  - 버튼이 비활성화되어 중복 실행을 방지합니다.

### [중단] 버튼
- **기능**: 진행 중인 크롤링을 중지합니다.
- **사용법**: 크롤링 중에만 활성화됩니다.
- **참고**: 중단해도 현재까지 수집된 데이터는 유지됩니다.

### [엑셀로 저장] 버튼
- **기능**: 수집한 데이터를 엑셀 파일로 저장합니다.
- **사용법**: 크롤링 완료 후 활성화됩니다.
- **저장 형식**: `.xlsx` (Excel 2007+ 형식)

### [프로그램 종료] 버튼
- **기능**: 프로그램을 안전하게 종료합니다.
- **참고**: 크롤링 중이면 경고 메시지가 표시됩니다.

## 크롤링 개수 선택 가이드

| 개수 | 권장 용도 | 예상 소요 시간 |
|------|----------|---------------|
| 10개 | 테스트/빠른 확인 | 1-2분 |
| 20개 | 소규모 조사 | 2-4분 |
| 50개 | 일반적인 사용 (기본값) | 5-10분 |
| 100개 | 중규모 데이터 수집 | 10-20분 |
| 200개 | 대규모 데이터 수집 | 20-40분 |
| 500개 | 전체 데이터 수집 | 40분-1시간+ |

**참고**: 실제 소요 시간은 네트워크 상태와 서버 응답 속도에 따라 달라질 수 있습니다.

## 엑셀 파일 저장 위치

### 기본 저장 위치
- `data/` 폴더에 저장됩니다.
- 파일명 형식: `33m2_숙소목록_날짜_시간.xlsx`
- 예: `33m2_숙소목록_20260201_143022.xlsx`

### 다른 위치에 저장하기
1. [엑셀로 저장] 버튼 클릭
2. 파일 저장 다이얼로그에서 원하는 폴더 선택
3. 파일명 수정 가능
4. "저장" 클릭

## 자주 발생하는 오류 해결법

### 오류: "브라우저를 시작할 수 없습니다"
**원인**: Chrome 브라우저가 설치되지 않았거나 버전이 맞지 않음
**해결 방법**:
1. Chrome 브라우저 최신 버전 설치
2. 프로그램 재실행

### 오류: "페이지 접속에 실패했습니다"
**원인**: 인터넷 연결 문제 또는 웹사이트 점검 중
**해결 방법**:
1. 인터넷 연결 확인
2. 웹 브라우저로 직접 사이트 접속 테스트
3. 잠시 후 다시 시도

### 오류: "숙소 카드를 찾을 수 없습니다"
**원인**: 웹사이트 구조가 변경되었거나 페이지 로딩 실패
**해결 방법**:
1. 프로그램 재시작
2. 홈페이지 접속부터 다시 시도
3. 문제 지속 시 개발자에게 문의

### 오류: "파일 저장에 실패했습니다"
**원인**: 저장 경로 권한 문제 또는 파일명 문제
**해결 방법**:
1. 다른 폴더에 저장 시도
2. 파일명에서 특수문자 제거
3. 바탕화면이나 문서 폴더에 저장 시도

### 오류: "프로그램이 응답하지 않습니다"
**원인**: 크롤링 작업 부하 또는 메모리 부족
**해결 방법**:
1. 잠시 기다림 (크롤링 중일 수 있음)
2. [중단] 버튼으로 작업 중지
3. 프로그램 재시작

## 문제 발생 시 확인 사항

1. **로그 파일 확인**: `logs/crawler.log` 파일에서 상세 오류 내용 확인
2. **인터넷 연결**: 네트워크 상태 점검
3. **Chrome 브라우저**: 최신 버전인지 확인
4. **메모리**: 다른 프로그램 종료 후 재시도

## 팁

- 처음 사용 시 10개로 테스트해보세요.
- 크롤링 중에는 Chrome 창을 최소화하지 마세요.
- 로그 창에서 진행 상황을 확인하면 문제를 빨리 파악할 수 있습니다.
- 정기적으로 수집하면 엑셀 파일이 많아지므로 폴더 정리를 권장합니다.
